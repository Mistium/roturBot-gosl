def balance(*Connection c, object msg) (
  object user = getUserProfile(msg.user)

  if user.error != null (
    sendResponse(c, msg, user.error.toStr())
    return
  )

  sendResponse(c, msg, "Balance: " ++ user.currency)
)