[
  {
    "type": "function",
    "function": {
      "name": "get_user",
      "description": "Retrieve detailed information for a specific user on rotur",
      "parameters": {
        "type": "object",
        "properties": {
          "username": {
            "type": "string",
            "description": "The username of the user to retrieve, returns a user object"
          }
        },
        "required": [
          "username"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_timezone_info",
      "description": "Returns the current time and other timezone data for a given timezone identifier.",
      "parameters": {
        "type": "object",
        "properties": {
          "timezone": {
            "type": "string",
            "description": "A valid timezone in UTC format, eg. UTC+1 UTC-5"
          }
        },
        "required": [
          "timezone"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_current_time",
      "description": "Get the current time in UTC and several common time zones. Useful for time-related queries, scheduling, and understanding when events occurred.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "search_web",
      "description": "Search the web for information using a query string.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query to use. For example: 'weather in London' or 'JavaScript array methods'"
          }
        },
        "required": [
          "query"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "extract_page",
      "description": "Extract and return the contents of one or more webpages.",
      "parameters": {
        "type": "object",
        "properties": {
          "urls": {
            "type": "array",
            "description": "A list of URLs to extract content from.",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "urls"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "search_lore",
      "description": "Search the lore database for events, happenings, or historical context related to Mistium, Rotur members, or related communities.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search term or topic to find in the lore database, e.g., 'Mistium founding', 'Rotur user events', 'recent happenings'"
          }
        },
        "required": [
          "query"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_posts",
      "description": "Retrieve recent posts made by a specific user on rotur",
      "parameters": {
        "type": "object",
        "properties": {
          "username": {
            "type": "string",
            "description": "The username of the user whose posts to retrieve, returns a user object with posts"
          }
        },
        "required": [
          "username"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "search_posts",
      "description": "Search for posts made by users on rotur, will return posts that contain the exact query",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "The search query to use when looking for posts"
          }
        },
        "required": [
          "query"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "convert_timestamp",
      "description": "Convert a timestamp to a human-readable format.",
      "parameters": {
        "type": "object",
        "properties": {
          "timestamp": {
            "type": "integer",
            "description": "The timestamp to convert. In JS's Date.now() format"
          }
        },
        "required": [
          "timestamp"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "make_web_request",
      "description": "Make an HTTP request to any URL. Use this to interact with external APIs and services.",
      "parameters": {
        "type": "object",
        "properties": {
          "method": {
            "type": "string",
            "enum": [
              "GET",
              "POST",
              "PUT",
              "DELETE",
              "PATCH",
              "HEAD",
              "OPTIONS"
            ],
            "description": "The HTTP method to use"
          },
          "url": {
            "type": "string",
            "description": "The full URL to make the request to"
          },
          "headers": {
            "type": "object",
            "description": "HTTP headers to send with the request. For Authorization, use format {\"Authorization\": \"Bearer token\"} or {\"Authorization\": \"api_key\"}"
          },
          "body": {
            "type": "object",
            "description": "Request body for POST, PUT, and PATCH requests (will be JSON.stringify'd)"
          },
          "params": {
            "type": "object",
            "description": "Query parameters to append to the URL"
          }
        },
        "required": [
          "method",
          "url"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "silent_exit",
      "description": "Delete the bot's reply message and stop processing without sending a text response. Use this when you only want to add reactions and have nothing meaningful to say.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "describe_image",
      "description": "Describe an image given its URL. Use this when you need to understand what's in an image that a user has shared or referenced. You can get image URLs from message attachments in the context.",
      "parameters": {
        "type": "object",
        "properties": {
          "image_url": {
            "type": "string",
            "description": "The URL of the image to describe. Image URLs are shown in message context as [Images: url1, url2]"
          }
        },
        "required": [
          "image_url"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "calculate",
      "description": "Calculate a mathematical expression. Supports arithmetic, algebra, trigonometry, logarithms, and more complex math operations.",
      "parameters": {
        "type": "object",
        "properties": {
          "expression": {
            "type": "string",
            "description": "The mathematical expression to calculate. Examples: '2 + 2', 'sin(pi/2)', 'sqrt(16)', 'x^2 + 2*x + 1' where x=3"
          },
          "variables": {
            "type": "object",
            "description": "Optional variable values for algebraic expressions. Example: {\"x\": 3, \"y\": 5}"
          }
        },
        "required": [
          "expression"
        ]
      }
    }
  }
]